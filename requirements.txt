# Core dependencies for Topology Alchemy
# Part of the OPENTUNITY EU Project

# Data manipulation and analysis
pandas>=1.3.0
numpy>=1.20.0

# Power system analysis
pandapower>=2.10.0
pypowsybl>=0.25.0
networkx>=2.6.0

# File format support
openpyxl>=3.0.9          # Excel file handling
rdflib>=6.0.0            # RDF/XML for CIM formats

# Web and API support
requests>=2.27.0
aiohttp>=3.8.0
aiofiles>=0.8.0

# Data transformation
marshmallow>=3.14.0
jsonpath_ng>=1.5.3
python-jsonpath>=1.0.0

# Logging and utilities
coloredlogs>=15.0
colorama>=0.4.4          # Cross-platform colored terminal output (for console app)
powersystem_analysis-0.2.tar.gz
# NOTE: The following dependency is from a private repository and has been commented out
# Users will need to install it manually or use an alternative if required
# powersystem_analysis @ git+ssh://git@gitlabnew.depid.local/energia/powersystem-analysis.git

# Optional: GUI support (uncomment if needed)
# Note: tkinter is usually included with Python, but on some systems needs separate installation
# On Ubuntu/Debian: sudo apt-get install python3-tk
# tkinter